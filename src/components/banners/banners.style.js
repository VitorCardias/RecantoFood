import { COLORS, FONT_SIZE } from "../../constants/theme.js"

export const styles = {
    banner: {
        paddingRight: 10,
        marginTop: 15,
        marginBottom: 15,
    },
    icone: {
        width: 220,
        height: 120,
    },
    descricao: {
        fontSize: FONT_SIZE.xsm,
        color: COLORS.dark_gray,
        textAlign: "center"
    },
}