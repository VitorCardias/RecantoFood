import { COLORS, FONT_SIZE } from "../../constants/theme";

export const styles = {
    container: {
        alignItems: "center",
        width: "100%"
    },
    imagem: {
        width: 274,
        height: 166
    },
    texto: {
        fontSize: FONT_SIZE.md,
        color: COLORS.dark_gray,
        textAlign: "center"
    }
}
