import { COLORS, FONT_SIZE } from "../../constants/theme.js"

export const styles = {
    categoria: {
        paddingRight: 10,
        marginTop: 15,
    },
    icone: {
        width: 65,
        height: 65,
    },
    descricao: {
        fontSize: FONT_SIZE.xsm,
        color: COLORS.dark_gray,
        textAlign: "center"
    },
}